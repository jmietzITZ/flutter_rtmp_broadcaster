org.gradle.jvmargs=-Xmx1536M
android.useAndroidX=true
# Example-only: Flutter's app Gradle plugin and transitive example plugins still
# need legacy Android/Kotlin Gradle behavior with AGP 9.
# The plugin module keeps both options enabled in ../../android/gradle.properties.
android.builtInKotlin=false
android.newDsl=false
